.row-fluid
  .span9
    - @entries.each do |entry|
      = render_cell(:blog_entry, :preview, entry)

    = will_paginate(@entries, controller: :blog, action: :index)

  .span3.sidebar
    = render_cell(:blog, :sidebar, @blog, @entry)