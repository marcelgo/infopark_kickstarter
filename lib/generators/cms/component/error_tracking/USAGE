Description:
    Integrates the an error tracking provider. By default, "Airbrake" is used as a provider.

Example:
    `rails generate cms:component:error_tracking`
    `rails generate cms:component:error_tracking --provider=airbrake`

    Misc: Executes provider dependent generator:
          `rails generate cms:component:error_tracking:airbrake`