Description:
    Integrates the error notification service "Honeybadger". You can disable deployment notifications
    by using the parameter "--skip-deployment-notifications".

Example:
    `rails generate cms:component:error_tracking:honeybadger`
    `rails generate cms:component:error_tracking:honeybadger --skip-deployment-notification`

    Gems: honeybadger
    Configuration: extends config/custom_cloud.yml
    Initializer: config/initializers/honeybadger.rb
    Deployment: extends deploy/after_restart.rb