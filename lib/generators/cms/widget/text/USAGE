Description:
    Generates a text widget that displays a title, body and is sortable.

Example:
    CMS: obj_class TextWidget
    CMS: attribute title (html)
    CMS: attribute body (html)
    CMS: attribute sort_key (string)

    `rails generate cms:widget:text`