Description:
    Generates a text widget that displays a headline, content and is sortable.

Example:
    CMS: obj_class TextWidget
    CMS: attribute headline (string)
    CMS: attribute content (html)

    `rails generate cms:widget:text`