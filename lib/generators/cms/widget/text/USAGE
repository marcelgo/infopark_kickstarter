Description:
    Generates a text widget that displays a headline, body and is sortable.

Example:
    CMS: obj_class TextWidget
    CMS: attribute headline (string)
    CMS: attribute body (html)

    `rails generate cms:widget:text`