Description:
    Generates a tabs widget that displays horizontal tabs.

Example:
    CMS: obj_class TabsWidget
    CMS: obj_class Tab
           attribute headline (string)
           attribute content (string)

    `rails generate cms:widget:tabs`