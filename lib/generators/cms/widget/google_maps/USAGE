Description:
    Generates a component to show a google maps on the website. The generator
    adds a new obj_class for the map to the cms. With the argument
    --example it is possible to add an example map to the default homepage obj.

Example:
    CMS: obj_class BoxGoogleMaps
    CMS: attribute google_maps_address (string)
    CMS: attribute google_maps_map_type (enum)

    `rails generate cms:widget:google_maps`
    `rails generate cms:widget:google_maps --example`