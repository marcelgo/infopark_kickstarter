Description:
    Generates a widget to show a google maps on the website. The generator
    adds a new obj_class for the map. With the argument
    --example it is possible to add an example map to the default homepage obj.

Example:
    CMS: obj_class GoogleMapsWidget
    CMS: attribute google_maps_address (string)
    CMS: attribute body (html)
    CMS: attribute title (string)

    `rails generate cms:widget:google_maps`
    `rails generate cms:widget:google_maps --example`