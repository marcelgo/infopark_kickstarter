= form_tag(cms_path(@search_page), method: :get, class: 'navbar-search pull-right form-search') do
  .input-append
    = text_field_tag(:q, @query, class: 'input-small search-query')

    = submit_tag(t('search.button'), class: 'btn')